language: python
before_install:
    - python --version
    - pip    --version
    - pytest --version
    - pip install pytest-cov
    - pip install codecov
script:
    - pytest --cov-report term --cov=nsp_lis
after_success:
    - codecov
