language: python
before_install:
    - python --version
    - pip    --version
    - pytest --version
    - pip install pytest-cov
    - pip install codecov
script:
    - PYTHONPATH=. pytest --cov-report term --cov=sym_lis
after_success:
    - codecov
